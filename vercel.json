{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "npm install -g pnpm && pnpm install",
        "buildCommand": "pnpm run build"
      }
    }
  ],
  "framework": "astro"
}
